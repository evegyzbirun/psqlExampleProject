<h1>All Sales Vinyl</h1>

<h3>Available Records</h3>

<% if @albums.any? %>
  <ul>
    <% @albums.each do |album| %>
      <li><a href="/albums/<%= album.id %>"><%= album.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p> there is no albums to show </p>
<% end %>

<h3>Records Sold :</h3>

<% if @albums_sold.any? %>
  <ul>
    <% @albums_sold.each do |album| %>
      <li><%= album.name %></li>
    <% end %>
  </ul>
<% else %>
  <p> there is no albums to show </p>
<% end %>


<a href="/albums/new">Add a new album</a>


<form action="/albums">
  <div class="form-group">
    <label for="search">Search Query</label>
    <input id="search" name="search" class="form-control" type="text">
  </div>
  <button type="submit" class="btn btn-success">Search!</button>
</form>
<br>
<br>
<p>for store info: <a href="/albums/info">click here</a></p>


