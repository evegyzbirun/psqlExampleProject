<h2>Album Name: <%= @album.name %></h2>

<h3>Album Songs</h3>

  <% if @album.songs.any? %>
    <ul>
      <% @album.songs.each do |song| %>
        <li><a href="/albums/<%= @album.id %>/songs/<%= song.id %>"><%= song.name %></a></li>
      <% end %>
    </ul>
  <% else %>
    <p>There are no songs listed for this album yet.</p>
  <% end %>

<h4>Add a song:</h4>

<form action="/albums/<%= @album.id %>/songs" method="post">
  <div class="form-group">
    <label for="artist_name">Song name</label>
    <input id="artist_name" name="artist_name" class="form-control" type="text">
  </div>
  <button type="submit" class="btn btn-success">Add artist</button>
</form>

<p><a href="/albums/<%= @album.id %>/edit">Edit artist</a></p>
<p><a href="/albums">Return to artists list</a></p>